# Design notes

Visual lock: the Attio-inspired landing screenshot (lilac bloom, screenshot product card, Inter). Do not restyle into another product’s palette or type.

**Motion:** follow [`design/animations-rules.md`](./animations-rules.md). Version A lock (Pattern B/C, lilac, screenshot card). Landing: black titles + hero H1 split-text; grey secondaries float as one block; step/fee/apply cards spatial-expand with no inner text motion. Hover 150–200ms ease-out. `prefers-reduced-motion` in CSS and JS.
